import { EasyListForDirective } from './easy-list-for.directive';

describe('EasyListForDirective', () => {
  it('should create an instance', () => {
    const directive = new EasyListForDirective();
    expect(directive).toBeTruthy();
  });
});
